# Bugfix 变更

> 状态: Active

本目录用于记录缺陷修复的复现, 根因, 修复与回归验证.

## 命名规则

- 新增文档文件名必须使用三位编号前缀(从 `001` 开始递增): `NNN-short-title.md`.
- 如修复跨多 PR 推进, 可升级为 `NNN-short-title-plan.md` + `NNN-short-title-progress.md`.

## 写作规范

详见: `../../Obsidian/standards/doc/changes-standards.md`.
