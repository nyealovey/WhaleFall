# Obsidian Vault

本目录是 WhaleFall 仓库内的 Obsidian vault(包含 `.obsidian/` 配置).

## 约定

- WhaleFall 后续自动/脚本生成的 `.canvas` 文件, 统一放在 `docs/Obsidian/canvas/`.
- `.canvas` 内的 `file` 路径必须是 vault-relative(相对于 `docs/Obsidian/`), 不要写仓库根路径前缀.
