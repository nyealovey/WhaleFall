flowchart TB
    subgraph 用户层["🌐 用户访问层"]
        Web["浏览器/Web App"]
        Mobile["移动App"]
        IoT["IoT设备"]
        Third["第三方系统"]
    end
    subgraph 接入层["⚡ 流量接入层"]
        DNS["DNS解析"]
        CDN["CDN加速"]
        WAF["WAF防火墙"]
        LB["负载均衡\nNginx/HAProxy"]
    end
    subgraph 网关层["🚪 网关层"]
        APIGW["API网关"]
        Auth["认证中心\nOAuth2/JWT"]
        RateLimit["限流熔断"]
    end
    subgraph 服务层["🔧 微服务层"]
        subgraph K8S["Kubernetes集群"]
            Master["Master节点\nAPI Server/Scheduler"]
            Worker1["Worker-1"]
            Worker2["Worker-2"]
            WorkerN["Worker-N"]
        end
        
        subgraph Services["业务服务"]
            SVC1["用户服务"]
            SVC2["订单服务"]
            SVC3["支付服务"]
            SVC4["通知服务"]
        end
        
        Mesh["Service Mesh\nIstio/Envoy"]
    end
    subgraph 数据层["💾 数据存储层"]
        subgraph 关系型DB["关系型数据库"]
            MySQL["MySQL\n主从集群"]
            PostgreSQL["PostgreSQL"]
        end
        
        subgraph NoSQL["NoSQL数据库"]
            Mongo["MongoDB"]
            ES["Elasticsearch\n搜索/日志"]
            Redis["Redis Cluster\n缓存"]
        end
        
        subgraph 消息队列["消息中间件"]
            Kafka["Kafka\n流处理"]
            RabbitMQ["RabbitMQ"]
            RocketMQ["RocketMQ"]
        end
    end
    subgraph 基础设施层["🏗️ 基础设施层"]
        subgraph IaaS["IaaS资源"]
            VM["虚拟机\nECS/GCE/Azure VM"]
            BM["裸金属服务器"]
            Container["容器运行时\nDocker/containerd"]
        end
        
        subgraph 网络["网络设施"]
            VPC["VPC虚拟网络"]
            Subnet["子网划分"]
            NAT["NAT网关"]
            VPN["VPN专线"]
            SDWAN["SD-WAN"]
        end
        
        subgraph 存储["存储设施"]
            Block["块存储\n云盘/SSD"]
            Object["对象存储\nS3/OSS"]
            File["文件存储\nNAS"]
        end
    end
    subgraph 运维监控层["📊 可观测性平台"]
        subgraph 监控["监控告警"]
            Prometheus["Prometheus\n指标采集"]
            Grafana["Grafana\n可视化"]
            Zabbix["Zabbix"]
        end
        
        subgraph 日志["日志管理"]
            ELK["ELK Stack"]
            Loki["Grafana Loki"]
            Fluentd["Fluentd采集"]
        end
        
        subgraph 链路追踪["APM链路追踪"]
            Jaeger["Jaeger"]
            SkyWalking["SkyWalking"]
            Zipkin["Zipkin"]
        end
        
        subgraph 自动化["DevOps自动化"]
            GitLabCI["GitLab CI/CD"]
            Jenkins["Jenkins"]
            ArgoCD["ArgoCD\nGitOps"]
            Ansible["Ansible"]
            Terraform["Terraform\nIaC"]
        end
    end
    %% 连接关系
    Web --> DNS
    Mobile --> DNS
    IoT --> DNS
    Third --> APIGW
    
    DNS --> CDN
    CDN --> WAF
    WAF --> LB
    LB --> APIGW
    
    APIGW --> Auth
    Auth --> RateLimit
    RateLimit --> SVC1
    RateLimit --> SVC2
    RateLimit --> SVC3
    RateLimit --> SVC4
    
    SVC1 --> Redis
    SVC2 --> MySQL
    SVC3 --> Kafka
    SVC4 --> ES
    
    Services --> Mesh
    Mesh --> K8S
    
    K8S --> VM
    K8S --> Container
    
    VM --> VPC
    VPC --> Subnet
    
    MySQL --> Block
    ES --> Object
    
    Prometheus --> Grafana
    ELK --> Kibana
    Jaeger --> SkyWalking